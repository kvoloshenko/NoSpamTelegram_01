import logging
from dotenv import load_dotenv
import os
from langchain_ollama import ChatOllama
from langchain_core.messages import HumanMessage

load_dotenv()

LOCAL_LLM = os.getenv('LOCAL_LLM')
print(f'LOCAL_LLM ={LOCAL_LLM }')

logger = logging.getLogger(__name__)

async def check_spam(topic: str) -> bool:
    # prompt = """Ты - система определения спама в чатах Telegram.
    # Твоя задача - анализировать сообщения и определять, являются ли они спамом.
    #
    # Характеристики спам-сообщений:
    # 1. Предложения быстрого заработка с нереалистично высокими суммами.
    # 2. Призывы к действию типа "пишите в личные сообщения", "ограниченное предложение".
    # 3. Намеренные орфографические ошибки или замена букв символами (например, "сOoбщение" вместо "сообщение").
    # 4. Неконкретные предложения работы без деталей.
    # 5. Обещания легкого заработка, "свободного графика" без объяснения сути работы.
    # 6. Использование эмодзи или необычных символов для привлечения внимания.
    # 7. Отсутствие контекста или связи с предыдущими сообщениями в чате.
    #
    # Проанализируй предоставленное сообщение и определи, является ли оно спамом на основе этих критериев.
    #
    # Отвечай только "SPAM", если сообщение соответствует характеристикам спама,
    # или "NOT_SPAM", если это обычное сообщение.
    # Не добавляйте никаких дополнительных комментариев или объяснений.
    # Вот текст сообщения: {question}"""

#     prompt = """Ты - система определения спама в чатах Telegram.
# Твоя задача - анализировать сообщения и определять, являются ли они спамом.
#
# Характеристики спам-сообщений:
# 1. Предложения быстрого заработка с указанием нереалистично высоких сумм за короткий срок (например, "$700 в неделю", "от 250$ в день").
# 2. Призывы к действию:
#    - "пишите в ЛС/личные сообщения", "оставляйте плюс", "напиши старт";
#    - требование отправить символ (например, "+") для получения информации;
#    - упоминание ограниченного количества мест ("нужны 10 человек", "мест мало").
# 3. Использование смеси кириллицы и латинских букв, символов (например, "BHИMAHИE", "cooбщeния"), а также избыточная пунктуация (‼️, —, •).
# 4. Отсутствие конкретики:
#    - расплывчатые формулировки ("направление белое", "интересная система");
#    - упоминание "официальных сайтов" или "прозрачности" без деталей;
#    - обещания "обучения" или "быстрого старта" без объяснений.
# 5. Структура в виде списка с тире/дефисами, разделением на псевдо-пункты.
# 6. Акцент на отсутствие рисков ("без обмана", "без вебкама") для манипуляции доверием.
# 7. Использование слов "удалённая занятость", "свободный график", "дополнительный доход" как ключевых маркеров.
#
# Проанализируй сообщение по этим критериям. Учитывай сочетание признаков: даже 2-3 совпадения могут указывать на спам.
#
# Отвечай только "SPAM", если сообщение соответствует характеристикам, или "NOT_SPAM".
# Никаких комментариев.
# Вот текст сообщения: {question}"""

#     prompt = """Ты — система определения спама в чатах Telegram.
# Твоя задача — анализировать сообщения и определять, являются ли они спамом.
#
# ### **Ключевые признаки спама:**
# 1. **Неестественно высокий доход за короткий срок**
#    - Указание конкретных сумм в день/неделю (например, "100-140$ в день", "от 700$ в неделю").
#    - Использование диапазонов ("от X до Y$") для создания иллюзии гибкости.
#
# 2. **Призывы к скрытому общению**
#    - "Пишите в ЛС/личку", "напишите '+'", "оставьте старт", "расскажу в личных".
#    - Упоминание ограниченного количества мест ("нужны 2–3 человека", "мест мало").
#
# 3. **Размытые условия и отсутствие деталей**
#    - Общие фразы: "онлайн-заработок", "хороший доход", "без жестких условий".
#    - Упоминание "направления" без объяснений ("расскажу, что за направление").
#    - Подчеркивание "официальности" или "прозрачности" без доказательств.
#
# 4. **Манипулятивные уточнения**
#    - "Без обмана", "без вебкама", "не продажи" — попытки снять возражения.
#    - Требование возраста/устройства ("от 20 лет", "с телефона или компьютера").
#
# 5. **Структурные маркеры**
#    - Списки с тире/дефисами, переносами строк.
#    - Избыток восклицательных знаков, эмодзи (‼️, 💰).
#    - Смешанные алфавиты ("Пpивeт", "cooбщeниe").
#
# ### **Как анализировать:**
# - **SPAM** — если есть **2+ признака** из списка (особенно доход + призыв в ЛС).
# - **NOT_SPAM** — если сообщение осмысленное, без скрытых манипуляций.
#
# **Отвечай только "SPAM" или "NOT_SPAM". Никаких пояснений.**
#
# Сообщение для проверки:
# {question}  """

#     prompt =  """Ты — система определения спама в чатах Telegram.
# Твоя задача — анализировать сообщения и определять, являются ли они спамом.
#
# ### **Ключевые признаки спама (SPAM):**
# 1. **Финансовые обещания**
#    - Указание конкретных сумм в день/неделю ("350$", "2000$ в неделю").
#    - Диапазоны ("от X до Y$") или гарантии ("ежедневный доход").
#
# 2. **Скрытые условия**
#    - Призывы писать в ЛС/личку ("Пишите + в ЛС", "по вопросам в сообщения").
#    - Ограничение мест ("нужно 5 человек", "берём 7-8").
#
# 3. **Размытые формулировки**
#    - "Достойный заработок", "отличный формат", "сотрудничество онлайн".
#    - Акцент на "удалённость" или "совмещение" без деталей.
#
# 4. **Манипуляции**
#    - "Без опыта", "достаточно телефона", "час в день" — минимизация усилий.
#    - Смешанные алфавиты ("дocтoйный", "oпытa").
#
# 5. **Структура**
#    - Списки с тире/точками, переносы строк.
#    - Избыток восклицаний/вопросов ("Заинтересованы?").
#
# ### **Исключения (NOT_SPAM):**
# 1. **Технические термины**
#    - AI, ИИ, Python, LLM, GPU/CPU, нейросети, "GPUStack".
#    - Профессиональный сленг ("инференс", "флант", "квантование").
#
# 2. **Осмысленные вопросы**
#    - Обсуждение кода, алгоритмов, железа, фреймворков.
#    - Сообщения без финансовых призывов и скрытых условий.
#
# ### **Правила анализа:**
# - **SPAM**: 2+ признака из раздела "SPAM" **и** отсутствие технических терминов.
# - **NOT_SPAM**: Любое упоминание AI/GPU/программирования **или** отсутствие маркеров спама.
#
# **Отвечай только "SPAM" или "NOT_SPAM". Никаких пояснений.**
#
# Сообщение для проверки:
# {question} """

#     prompt = """Ты — высокоточная система детекции спама для Telegram. Анализируй сообщения строго по нижеуказанным правилам.
#
# ### Критерии СПАМА (отвечать "SPAM"):
# 1. **Финансовые обещания**
#    - Любые конкретные суммы ("350$", "от 500$", "2000$ в неделю")
#    - Указание временного периода ("в день", "ежедневно", "за неделю")
#    - Фразы: "доход от", "прибыль", "заработок", "получайте"
#
# 2. **Скрытое взаимодействие**
#    - Прямые призывы: "пишите в ЛС", "в сообщения", "напишите мне"
#    - Косвенные призывы: "заинтересованы?", "интересно?", "хотите узнать?"
#    - Требование действий: "оставьте +", "напишите 'старт'"
#
# 3. **Расплывчатые предложения**
#    - "Отличный формат", "интересное предложение", "выгодные условия"
#    - "Сотрудничество", "удалённая работа" без конкретики
#    - "Достойный доход", "хороший заработок" без деталей
#
# 4. **Структурные маркеры**
#    - Сочетание финансовых цифр + вопроса + призыва к действию
#    - Использование точек/тире вместо нормальных предложений
#
# ### Исключения (отвечать "NOT_SPAM"):
# 1. **Техническая лексика**
#    - AI/ИИ, программирование (Python, C++), нейросети
#    - Оборудование (GPU, CPU, сервера), фреймворки
#    - Профессиональные термины ("инференс", "трансформер")
#
# 2. **Нормальная коммуникация**
#    - Вопросы/обсуждения без финансового подтекста
#    - Сообщения с конкретной технической информацией
#    - Профессиональные дискуссии любого рода
#
# ### Особые указания:
# 1. Любое сочетание "конкретная сумма + призыв к действию" = SPAM
# 2. Технические термины перевешивают спам-маркеры
# 3. Короткие сообщения ("GPUStack") не считаются спамом
#
# Формат ответа ТОЛЬКО:
# SPAM
# ИЛИ
# NOT_SPAM
#
# Анализируемое сообщение:
# {question}"""

    # prompt = """
    # Ты — система детекции спама для Telegram-чатов. Твоя задача — по строгим правилам решать, является ли сообщение откровенным спамом-рекрутингом/«лёгким заработком».
    #
    # ====================================================================
    # 🟥 1. ПРИЗНАКИ СПАМА  (если выполняются ≥ 2 пунктов → ответ «SPAM»)
    # --------------------------------------------------------------------
    # A. Финансовая нажива
    #    • Любые слова про доход/заработок/деньги/прибыль/инвестиции/доходность/активы/«крипта».
    #    • Конкретные суммы **или** расплывчатые обещания («от 700 $ в неделю», «без ограничений», «пассивный доход»).
    #
    # B. Призыв связаться лично
    #    • «Пишите в ЛС», «жду + в личку», «ответьте в директ», @ник с просьбой написать.
    #    • Тон формулы «расскажу только тем, кто…», «обсудим детали лично».
    #
    # C. Недосказанные условия / ограниченный оффер
    #    • «Нужно 5 человек», «только сегодня», «кол-во мест ограничено».
    #    • Обучение/шаблон «без опыта», «пошаговое обучение», «карьерный рост».
    #
    # D. Отсутствие конкретики (более 50 % текста — общие фразы)
    #    • Нет чётких технических деталей, ссылок на ресурсы, названий компаний/технологий.
    #
    # E. Криптовалюта/цифровые активы **+** обещание дохода
    #    • Любые «цифровые активы», «депозит на кошельке», «не доверительное управление» и т. д.
    #
    # F. Формула «работа/подработка/доп. заработок/партнёрство» без явной профессии
    #    • «Ищу людей…», «предлагаю партнёрство…», «удалённо/дистанционно».
    #
    # ====================================================================
    # 🟩 2. ИСКЛЮЧЕНИЯ  (приоритет «NOT_SPAM», даже если есть маркеры выше)
    # --------------------------------------------------------------------
    # 1. **Технический/профессиональный контекст**
    #    • AI/LLM, программирование (Python, GPU, API), тестирование, данные, инференс, квантование, «трансформер», devops и т. п.
    # 2. **Осмысленное обсуждение**
    #    • Анализ проблем, кейсы, эксперименты, доказательства, ссылки на исследование/репозиторий.
    # 3. **Допустимые шаблоны**
    #    • Нумерованные тех-списки, цитаты моделей, неформальная пунктуация в тех-контексте.
    #
    # ====================================================================
    # ⚖️ 3. РЕШАЮЩИЕ ПРАВИЛА
    # --------------------------------------------------------------------
    # 1. Сначала ищи технические сигналы раздела 2 → если нашёл ≥ 1 — сразу «NOT_SPAM».
    # 2. Иначе проверь раздел 1: если выполняются минимум **два** подпункта A-F — «SPAM».
    # 3. Если сомневаешься (ровно один признак) — выбирай «NOT_SPAM».
    #
    # ====================================================================
    # Формат ответа: **ТОЛЬКО** «SPAM» **или** «NOT_SPAM».
    # Сообщение: {question}
    # """

    # prompt = """
    # Ты — система детекции спама для Telegram-чатов. Твоя задача — по строгим правилам решать, является ли сообщение откровенным спамом-рекрутингом/«лёгким заработком».
    #
    # ====================================================================
    # 🟥 1. ПРИЗНАКИ СПАМА  (если выполняются ≥ 2 пунктов → ответ «SPAM»)
    # --------------------------------------------------------------------
    # A. Финансовая мотивация
    #    • Любые упоминания: доход/заработок/деньги/прибыль/инвестиции/активы/крипта/кошелек/дивиденты.
    #    • Конкретные суммы ($, €, ₽) ИЛИ расплывчатые обещания:
    #      - "от X$", "стабильно", "легально", "пассивный доход", "доп. заработок", "финансовая свобода"
    #      - "резко интереснее", "перспективное направление", "гарантированно", "в своём ритме"
    #
    # B. Призыв к личному контакту
    #    • Прямые указания: "пишите в ЛС", "жду + в личку", "ответьте в директ", "напиши 'хочу'", "@ник", "обсудим лично"
    #    • Косвенные призывы: "расскажу только тем, кто...", "подробности в сообщениях", "дальше в переписке"
    #
    # C. Неопределённые условия
    #    • Ограничения: "только N человек", "кол-во мест ограничено", "только сегодня/сейчас"
    #    • Неясный процесс: "новое направление", "партнёрство", "пошаговое обучение", "карьерный рост", "сам себе начальник"
    #
    # D. Отсутствие конкретики (>60% текста)
    #    • Нет: названий компаний, технологий, ссылок, специфичных терминов
    #    • Есть: общие фразы ("всё по-честному", "в своём ритме", "интересные возможности")
    #
    # E. Крипто-контекст + доход
    #    • "цифровые активы", "депозит на кошельке", "не доверительное управление", "NFT", "блокчейн" + обещание прибыли
    #
    # F. Формат работы без специфики
    #    • "удалённо/дистанционно/онлайн", "для всех", "без опыта", "в свободное время", "подработка", "дома"
    #
    # ====================================================================
    # 🟩 2. ИСКЛЮЧЕНИЯ  (приоритет «NOT_SPAM», даже если есть маркеры выше)
    # --------------------------------------------------------------------
    # 1. **Технический контент**
    #    • AI/LLM, программирование (API, Python, GPU), данные, инференс, квантование, трансформеры, DevOps
    #    • Конкретные инструменты: Git, Docker, TensorFlow, PyTorch, SQL
    #
    # 2. **Профессиональное обсуждение**
    #    • Кейсы с метриками/кодом, анализ ошибок, сравнение алгоритмов, ссылки на исследования/GitHub
    #    • Обсуждение моделей (Llama, GPT), аппаратного обеспечения, оптимизации
    #
    # 3. **Допустимые шаблоны**
    #    • Нумерованные списки с техническими пунктами, цитаты кода/логов, таблицы сравнений
    #
    # ====================================================================
    # ⚖️ 3. РЕШАЮЩИЕ ПРАВИЛА
    # --------------------------------------------------------------------
    # 1. Сначала проверь раздел 2 → если есть ЛЮБОЙ элемент → «NOT_SPAM».
    # 2. Иначе считай триггеры из раздела 1:
    #    - ≥2 триггеров → «SPAM»
    #    - 1 триггер → «NOT_SPAM»
    #    - 0 → «NOT_SPAM»
    #
    # 3. Особые случаи:
    #    - Сочетание "деньги" + "писать в ЛС" → всегда SPAM (даже если 1 триггер)
    #    - "Крипто" + "доход" → автоматически 2 триггера
    #
    # ====================================================================
    # Формат ответа: ТОЛЬКО SPAM или NOT_SPAM.
    # Сообщение: {question}
    # """

    # prompt = """
    # Ты — система детекции спама для Telegram-чатов. Твоя задача — по строгим правилам решать, является ли сообщение откровенным спамом-рекрутингом/«лёгким заработком».
    #
    # ====================================================================
    # 🟥 1. ПРИЗНАКИ СПАМА  (если выполняются ≥ 2 пунктов → ответ «SPAM»)
    # --------------------------------------------------------------------
    # A. Финансовая нажива
    #    • Упоминания: доход/заработок/деньги/прибыль/инвестиции/активы/крипта/кошелёк/легально/стабильно/резко интереснее.
    #    • Конкретные суммы ($, €, баксов) ИЛИ расплывчатые обещания («от 700$», «без ограничений», «пассивный доход», «с первого дня», «в своём ритме»).
    #
    # B. Призыв связаться лично
    #    • Прямые указания: «Пишите в ЛС», «жду +», «ответьте в директ», «дам информацию», «обсудим детали», «напиши "хочу"», «пишите плюс».
    #    • Косвенные призывы: «расскажу только тем, кто…», «кто готов?», «если есть интерес», «хочешь?», «за подробностями...».
    #
    # C. Лимитированные/непрозрачные условия
    #    • Ограничения: «нужно N человек», «только сегодня», «2 места», «кол-во ограничено».
    #    • Обучение/условия: «без опыта», «пошаговое обучение», «сам себе начальник», «всё под вашим контролем», «делитесь своим опытом».
    #
    # D. Отсутствие конкретики (>50% текста)
    #    • Общие фразы: «перспективное направление», «цифровые активы», «в своём ритме», «всё по-честному», «доп. заработок», «партнёрство».
    #    • Нет технических деталей, названий компаний, ссылок, специфичных терминов.
    #
    # E. Крипто/инвестиционный контекст
    #    • Любые упоминания: «цифровые активы», «кошелёк», «не доверительное управление», «крипта», «депозит», «активы твои».
    #
    # F. Шаблонные схемы
    #    • Профессиональная неопределённость: «удалённо/дистанционно», «работаешь на дядю», «доп. заработок», «мини-офис», «телефон/онлайн».
    #    • Эмоциональные контрасты: «было Х → стало Y», «когда-то я... теперь сама так живу», «грустно → интереснее».
    #
    # ====================================================================
    # 🟩 2. ИСКЛЮЧЕНИЯ  (приоритет «NOT_SPAM», даже если есть маркеры выше)
    # --------------------------------------------------------------------
    # 1. **Технический/профессиональный контекст**
    #    • AI/LLM, API, Python, GPU, трансформеры, тестирование, инференс, квантование, DevOps.
    # 2. **Конкретные обсуждения**
    #    • Кейсы с техническими параметрами, версиями ПО, экспериментами, ссылками на репозитории/исследования.
    # 3. **Допустимые шаблоны**
    #    • Нумерованные списки в тех-контексте, цитаты кода, обсуждение моделей/архитектур.
    #
    # ====================================================================
    # ⚖️ 3. РЕШАЮЩИЕ ПРАВИЛА
    # --------------------------------------------------------------------
    # 1. Сначала проверь раздел 2 → если есть ≥1 сигнала → «NOT_SPAM».
    # 2. Иначе проверь раздел 1:
    #    - Если ≥2 признаков A-F → «SPAM».
    #    - Если ровно 1 признак → «NOT_SPAM».
    #    - Даже 1 признак из E → комбинируй с другими.
    #
    # ====================================================================
    # Формат ответа: Отвечай без think ТОЛЬКО SPAM или NOT_SPAM.
    # Сообщение: {question}
    # """

    # prompt = """
    # Ты — система детекции спама для Telegram-чатов. Твоя задача — по строгим правилам решать, является ли сообщение откровенным спамом-рекрутингом/«лёгким заработком».
    #
    # ====================================================================
    # 🟥 1. ПРИЗНАКИ СПАМА (≥ 2 пунктов → «SPAM»)
    # --------------------------------------------------------------------
    # A. Финансовая нажива
    #    • Упоминания: доход/заработок/деньги/прибыль/легально/стабильно/результат.
    #    • Конкретные суммы ($, €, баксы, USD) В ЛЮБОЙ ФОРМЕ (200$, 500 долларов, 180 баксов) ИЛИ расплывчатые обещания («от X$», «без опыта», «с первого дня», «в своём ритме», «ежедневно»).
    #
    # B. Призыв связаться лично
    #    • Прямые указания: «Пишите в ЛС/личные», «жду +», «ответьте в директ», «объясню», «расскажу подробнее», «пиши "хочу"/"старт"», «напишите плюс», «дальше в переписке», «для более подробной информации».
    #    • Косвенные призывы: «кто готов?», «хочешь так же?», «заинтересовало?», «решай сам», «если интересно».
    #
    # C. Лимитированные/непрозрачные условия
    #    • Ограничения: «нужно N человек», «открыто X мест», «кол-во ограничено», «2-3 человека», «только сегодня», «быстрое начало».
    #    • Условия: «без опыта», «удалённо/дистанционно», «всё просто», «телефон/онлайн», «мини-офис», «сам себе начальник».
    #
    # D. Эмоциональные манипуляции
    #    • Контрасты: «Было Х → стало Y», «когда-то я... теперь так живу», «работаешь на дядю vs на себя».
    #    • Личные истории: «мой телефон — мой офис», «не упусти шанс», «теперь сама так живу», «хочешь перемен?».
    #
    # ====================================================================
    # 🟩 2. ИСКЛЮЧЕНИЯ (приоритет «NOT_SPAM»)
    # --------------------------------------------------------------------
    # 1. **Технический/профессиональный контекст**
    #    • Обсуждение IT/инжиниринга: Python, API, DevOps, ML модели, тестирование, алгоритмы.
    #    • Конкретные кейсы с параметрами, версиями ПО, ссылками на GitHub/исследования.
    #
    # ====================================================================
    # ⚖️ 3. РЕШАЮЩИЕ ПРАВИЛА
    # --------------------------------------------------------------------
    # 1. Если есть ИСКЛЮЧЕНИЯ → «NOT_SPAM».
    # 2. Иначе:
    #    - ≥2 признаков из A-D → «SPAM».
    #    - 1 признак → «NOT_SPAM».
    #    - Даже 1 признак + эмоциональные манипуляции (D) → «SPAM».
    #
    # ====================================================================
    # Формат ответа: ТОЛЬКО SPAM или NOT_SPAM.
    # Сообщение: {question}
    # """

    # prompt = """
    # Ты — строгий фильтр спама для Telegram. Анализируй сообщения по критериям. Ответ только SPAM/NOT_SPAM.
    #
    # ==================================================
    # 🟥 КРИТЕРИИ СПАМА (≥2 из A-D → SPAM)
    # --------------------------------------------------
    # A. ДЕНЕЖНЫЕ ОБЕЩАНИЯ
    #    • Конкретные суммы: "$", "доллар", "бакс", "€", "в день/неделю/месяц"
    #    • Даже если сумма словами: "семьсот долларов", "две тысячи"
    #    • Фразы: "легально", "стабильно", "результат", "ежедневно", "гарантированно"
    #
    # B. ЛИЧНЫЙ КОНТАКТ
    #    • Прямой призыв:
    #      - "пишите в ЛС/личку/директ"
    #      - "напиши '+'/старт/хочу"
    #      - "расскажу в переписке"
    #      - "за подробностями..." (даже без явного указания)
    #    • Сокращения: "ЛС", "лс", "плюс в личку", "приват"
    #
    # C. ЛИМИТЫ И УСЛОВИЯ
    #    • Квоты:
    #      - "возьму N человек", "нужно X людей", "открыто Y мест"
    #      - "2-3 человека", "кол-во ограничено", "последние места"
    #    • Условия работы:
    #      - "удалённо/дистанционно", "без опыта", "всё просто"
    #      - "телефон/онлайн", "сам себе начальник", "мини-офис"
    #
    # D. ЭМОЦИОНАЛЬНЫЕ КРЮЧКИ
    #    • Контрасты:
    #      - "Работать на дядю vs на себя"
    #      - "Было трудно → стало легко"
    #    • Личные истории:
    #      - "Раньше не верила, теперь сама так живу"
    #      - "Мой телефон = мой офис"
    #
    # ==================================================
    # 🟩 ИСКЛЮЧЕНИЯ (ТОЛЬКО технический контент → NOT_SPAM)
    # --------------------------------------------------
    # • Обсуждение IT/инженерии: Python, API, модели ML, DevOps
    # • Конкретные термины: "трансформеры", "инференс", "CUDA", "GitHub"
    # • Ссылки на репозитории/исследования, версии ПО, параметры моделей
    #
    # ==================================================
    # ⚖️ ПРАВИЛА РЕШЕНИЯ
    # --------------------------------------------------
    # 1. Есть ИСКЛЮЧЕНИЯ → NOT_SPAM
    # 2. Иначе:
    #    - (A И B) → SPAM (даже без других признаков)
    #    - (A ИЛИ B) + (C ИЛИ D) → SPAM
    #    - ≥2 любых признаков → SPAM
    #
    # ==================================================
    # Сообщение: {question}
    # Ответ:"""

    # prompt = """
    # Ты — строгий фильтр спама для Telegram. Анализируй сообщения по критериям. Ответ только SPAM/NOT_SPAM.
    #
    # ==================================================
    # 🟥 КРИТЕРИИ СПАМА (SPAM если ≥2 пунктов)
    # --------------------------------------------------
    # A. ДЕНЕЖНЫЕ ОБЕЩАНИЯ
    #    • Любые упоминания: доход/делим результат/прибыль/проценты
    #    • Даже без суммы: "гарантия дохода", "без вложений", "пассивный заработок"
    #    • Финансовые модели: "модель 80/20", "краш-схема", "партнёрская программа"
    #
    # B. ПРИЗЫВ К КОНТАКТУ
    #    • Явные указания: "в ЛС", "пишите в личку", "обсудим приватно"
    #    • Скрытые триггеры: "кому интересно — ...", "дальше в переписке", "для избранных"
    #
    # C. ПСЕВДО-УСЛОВИЯ
    #    • Отрицания рисков: "без вложений/инвестиций/опыта", "ничего не теряете"
    #    • Ложная простота: "всё под вашим контролем", "просто повторяй", "1 час в день"
    #    • Фиктивные ограничения: "берём 5 человек", "последний шанс"
    #
    # D. МАРКЕТИНГОВЫЕ ШАБЛОНЫ
    #    • Бизнес-сленг: "пассивный доход", "монетизация", "модель сотрудничества"
    #    • Эмоциональные якоря: "это не лохотрон", "проверено лично", "тайная методика"
    #
    # ==================================================
    # 🟩 ИСКЛЮЧЕНИЯ (ТОЛЬКО если явный техконтент)
    # --------------------------------------------------
    # ✔️ Допустимо:
    #    - Обсуждение ML/DL моделей (трансформеры, GPT, диффузия)
    #    - Технические термины: API, CUDA, инференс, квантование
    #    - Конкретные IT-кейсы с параметрами/кодом/ссылками
    #
    # ❌ Не исключение:
    #    - Упоминания "модели" в финансовом контексте (80/20, партнёрская)
    #    - Общие IT-слова без специфики ("цифровизация", "онлайн-платформа")
    #
    # ==================================================
    # ⚖️ РЕШАЮЩИЕ ПРАВИЛА
    # --------------------------------------------------
    # 1. Сообщение содержит любые элементы A + B → SPAM
    # 2. Присутствует C + (A ИЛИ B) → SPAM
    # 3. Любые 2 критерия из A-D → SPAM
    # 4. Даже 1 критерий + финансовый контекст → SPAM
    #
    # ==================================================
    # Сообщение: {question}
    # Ответ:"""

    prompt = """
    Ты — антиспам-фильтр Telegram. Жёстко маркируй сообщения как SPAM при любых признаках "быстрых денег". Ответ только SPAM/NOT_SPAM.

    ==================================================
    🟥 КРИТЕРИИ СПАМА (SPAM если ≥1 из Core + 1 из Secondary)
    --------------------------------------------------
    **Core-триггеры (обязательные):**
    A. ДЕНЕЖНЫЕ ОБЕЩАНИЯ:
       • Проценты/доли: "80% прибыли", "50/50", "делим результат"
       • Даже без суммы: "гарантия дохода", "без вложений", "пассивно"
       • Схемы: "модель X/Y", "партнёрка", "сопровождение"

    B. ЯВНЫЙ ПРИЗЫВ:
       • "Только по запросу", "напишите слово...", "в ответ отправьте..."
       • Ссылки на контакт: "в телеграм", "в профиле", "через бота"

    **Secondary-триггеры:**
    C. ЛИМИТИРОВАНИЕ:
       • "Только N человек", "последние места", "закрытый набор"
       • Ограничения времени: "до завтра", "быстрый старт"

    D. МАНИПУЛЯЦИИ:
       • Отрицание рисков: "ничего не теряете", "без обязательств"
       • Псевдо-легальность: "всё официально", "прозрачный договор"

    ==================================================
    🟩 ИСКЛЮЧЕНИЯ (ТОЛЬКО если):
    --------------------------------------------------
    ✔️ Конкретные IT/инженерия:
       - Обсуждение кода (Python, SQL), ML моделей (трансформеры, RLHF)
       - Технические термины: API, CUDA, инференс, квантование
       - Ссылки на GitHub/GitLab, Jupyter ноутбуки, исследовательские статьи

    ❌ **Не исключение:**
       - Упоминания "модели" в финансовом контексте
       - Общие слова: "онлайн", "удалённо", "система"

    ==================================================
    ⚖️ РЕШАЮЩИЕ ПРАВИЛА:
    --------------------------------------------------
    1. **SPAM если:**
       - (A ИЛИ B) + (C ИЛИ D) → SPAM
       - Даже одно Core + один Secondary
       - Любые попытки обойти фильтр (замена символов: "в_ЛС", "лс")

    2. **NOT_SPAM только если:**
       - Чёткое соответствие ИСКЛЮЧЕНИЯМ (технический контент)

    ==================================================
    Сообщение: {question}
    Ответ:"""

    try:
        # Инициализация локальной языковой модели с параметрами
        # local_llm = "llama3.2:3b-instruct-fp16"
        # local_llm = "deepseek-r1:14b"
        llm = ChatOllama(model=LOCAL_LLM, temperature=0)

        # Формирование запроса для LLM
        # print(f'text={topic}')
        prompt_formatted = prompt.format(question=topic)
        # Отправка запроса (сформированного промпта) в модель и получение ответа
        generation = llm.invoke([HumanMessage(content=prompt_formatted)])
        model_response = generation.content
        print(f'model_response={model_response}')
        print(type(model_response))


        return model_response == "SPAM"
    except Exception as e:
        logger.error(f"Ошибка при проверке на спам: {e}")
        return False